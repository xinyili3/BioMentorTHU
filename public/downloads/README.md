# Downloads

Place downloadable files in this directory. Vite copies them to the site root during builds.

For example, `public/downloads/program-guide.pdf` is available from the page as
`/downloads/program-guide.pdf`.
